name: force_update
description: This package is use for check new version available both ios and android. If it has a new version, you can link to app store or play store.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4
  html: ^0.15.0
  package_info: ^2.0.2
  open_appstore: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg